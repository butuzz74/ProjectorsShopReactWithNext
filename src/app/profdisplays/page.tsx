import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Proffecional displays page | SOI',
  description: 'Create proffecional display page',
};

function Displays(): React.ReactNode {
  return <h1 className="text-black">Displays</h1>;
}

export default Displays;
